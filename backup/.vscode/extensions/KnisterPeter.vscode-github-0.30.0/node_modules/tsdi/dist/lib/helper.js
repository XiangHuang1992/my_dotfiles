"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function isFactoryMetadata(metadata) {
    return Boolean(metadata.rtti);
}
exports.isFactoryMetadata = isFactoryMetadata;
function findIndexOf(list, test) {
    var idx = -1;
    for (var i = 0, n = list.length; i < n; i++) {
        if (test(list[i])) {
            idx = i;
        }
    }
    return idx;
}
exports.findIndexOf = findIndexOf;
function removeElement(list, test) {
    var idx = findIndexOf(list, test);
    if (idx > -1) {
        return list.slice(0, idx).concat(list.slice(idx + 1));
    }
    return list;
}
exports.removeElement = removeElement;
function getNamedOptions(optionOrString) {
    if (typeof optionOrString === 'string') {
        var named = {
            name: optionOrString
        };
        return named;
    }
    return optionOrString;
}
exports.getNamedOptions = getNamedOptions;
//# sourceMappingURL=helper.js.map